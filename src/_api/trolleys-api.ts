import axios, { AxiosResponse } from "axios"

import { Trolley } from "../src/interfaces/trolley-interfaces"

export const addTrolley = async (trolley: Trolley): Promise<AxiosResponse> => {
	try {
		const resp = await axios.post
	} catch (error) {}
}
